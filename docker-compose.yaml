# Docker Compose for Vega Module
#
# Usage:
#   docker compose up --build        # Build and start
#   docker compose up -d             # Start in background
#   docker compose down              # Stop and remove
#   docker compose logs -f           # Follow logs

services:
  vega-node:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "2000:2000"
    environment:
      - PYTHONUNBUFFERED=1
    # Uncomment to use the fake (simulated) interface:
    # command: ["--fake"]

    # --- Common patterns (uncomment and modify as needed) ---

    # USB device passthrough (e.g., serial instruments):
    # devices:
    #   - /dev/ttyUSB0:/dev/ttyUSB0
    # privileged: true  # May be needed for some USB devices

    # Volume mounts for persistent data or configuration:
    # volumes:
    #   - ./data:/app/data
    #   - /dev/shm:/dev/shm  # Shared memory (useful for cameras)

    # Use the host network directly (alternative to port mapping):
    # network_mode: host
    # NOTE: Remove the "ports:" section above when using host network mode.

    # Connect to MADSci manager services:
    # environment:
    #   - EVENT_SERVER_URL=http://host.docker.internal:8001
    #   - RESOURCE_SERVER_URL=http://host.docker.internal:8003
    #   - DATA_SERVER_URL=http://host.docker.internal:8004

    # Restart policy for production:
    # restart: unless-stopped

    # Health check:
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:2000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
